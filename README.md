# IR Project

## Installation

```
pip install -r requirements.txt
```

## Usage

Run `index_construction.py` first so that the inverted index file and the keyed documents file are available for `search_interface.py` to run properly.

Afterwards, run `search_interface.py` and follow the printed instructions to navigate the CLI interface.
